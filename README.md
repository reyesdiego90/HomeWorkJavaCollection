# HomeWorkJavaCollection
How to run this project:
We need to have JDK, Java and Maven,
We need to run the next steps:
```
  # mvn install
  # cd .\target
  # java -jar .\HomeWork1-1.0-SNAPSHOT.jar
```
